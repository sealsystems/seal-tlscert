################################################################################
# OPEN SOURCE MODULE
# Remove the comment if you changed the build scripts!
################################################################################

machine:
  timezone: Europe/Berlin

  node:
    version: 6.10.1

dependencies:
  pre:
    - npm install -g roboter-cli

test:
  override:
    - npm run bot
